# pdfrotate

브라우저 안에서만 동작하는 클라이언트 사이드 웹 앱. 사용자가 PDF 파일을 선택하고 회전 버튼으로 미리보기를 원하는 방향에 맞춘 뒤, 다운로드 버튼으로 모든 페이지가 회전된 결과 PDF를 내려받는다. 파일은 서버로 전송되지 않는다.

## Language

**파일 선택 (File Selection)**:
사용자가 로컬 PDF 한 개를 브라우저에 여는 행위. 한 번에 하나의 문서만 다루며, 파일은 브라우저 메모리에만 존재하고 네트워크로 전송되지 않는다.
_Avoid_: 업로드(upload) — 서버 전송을 암시하므로 UI 문구·코드·문서 모두에서 금지. 다중 파일 일괄 처리 — 범위 밖

**클라이언트 사이드 처리 (Client-side Processing)**:
PDF 회전 연산이 전적으로 사용자의 브라우저에서 수행되는 방식. 서버·스토리지가 존재하지 않는다.

**전체 회전 (Whole-document Rotation)**:
문서 내 모든 페이지에 동일한 회전을 일괄 적용하는 것. 이 앱의 유일한 회전 단위이다.
_Avoid_: 페이지별 회전(per-page rotation) — 이 앱의 범위 밖

**회전 버튼 (Rotate Buttons)**:
시계 90°·반시계 90° 두 개의 아이콘 전용 버튼(텍스트 없음). 누를 때마다 누적 회전이 갱신되고 미리보기가 즉시 그 방향으로 다시 그려진다. 180°는 같은 버튼을 두 번 눌러 도달한다. 다운로드를 일으키지 않는다.

**누적 회전 (Accumulated Rotation)**:
회전 버튼 누름이 쌓여 만들어지는, 문서 전체에 적용될 각도. 정준 표기는 시계 방향 기준 도수(0 | 90 | 180 | 270)이며, 반시계 90°는 270으로 적는다. 0이면 내려받을 변화가 없으므로 다운로드 버튼이 비활성화된다.
_Avoid_: 자유 각도(임의 각도 입력) — PDF 표준상 90° 배수만 유효하므로 범위 밖. `-90`·`90ccw` 등 비정준 표기

**결과 파일 (Result File)**:
회전이 적용되어 다운로드되는 PDF. 이름은 `{원본명}-rotated-{각도}.pdf` (예: `scan-rotated-270.pdf`).

**상대 회전 (Relative Rotation)**:
회전 각도를 각 페이지의 기존 회전값에 더하는 방식. 사용자가 뷰어에서 보던 모습 기준으로 모든 페이지가 동일하게 돌아간다.
_Avoid_: 절대 회전(absolute rotation) — 회전값 덮어쓰기는 페이지마다 겉보기 결과가 달라지므로 금지

**다운로드 버튼 (Download Button)**:
화면에 단 하나뿐인 주 액션 버튼. 누르면 누적 회전이 문서 내 모든 페이지에 상대 회전으로 적용된 결과 파일이 다운로드된다.
_Avoid_: 즉시 다운로드(각도 선택 즉시 다운로드) — 이전 모델, 폐기됨

**보호 문서 (Protected Document)**:
열람 암호가 걸린 PDF. 이 앱은 지원하지 않으며, PDF가 아닌 파일·손상된 PDF와 마찬가지로 명확한 오류 메시지 후 다시 파일 선택으로 돌아간다.

**미리보기 (Preview)**:
파일 선택 직후 표시되는 최대 첫 10페이지의 썸네일. 가로 방향으로 슬라이드(스크롤)하며 넘겨 볼 수 있고, 누적 회전을 실시간으로 반영해 다운로드될 결과와 같은 방향을 미리 보여준다.
_Avoid_: 첫 페이지 한 장 미리보기 — 이전 모델, 폐기됨
