# pdfrotate

브라우저 안에서만 동작하는 클라이언트 사이드 웹 앱. 사용자가 PDF 파일을 선택하면 뷰어 모드로 전환되고, 미리보기 페이지를 원호 드래그로 비틀거나 회전 버튼을 눌러 방향을 맞춘 뒤, 다운로드 버튼으로 모든 페이지가 회전된 결과 PDF를 내려받는다. 파일은 서버로 전송되지 않는다.

디자인 헌법은 루트의 `apple-design.md`이다 (ADR-0001). 표현 계층의 모든 결정(재질·모션·타이포)은 이 문서를 근거로 한다.

## Language

**파일 선택 (File Selection)**:
사용자가 로컬 PDF 한 개를 브라우저에 여는 행위. 한 번에 하나의 문서만 다루며, 파일은 브라우저 메모리에만 존재하고 네트워크로 전송되지 않는다.
_Avoid_: 업로드(upload) — 서버 전송을 암시하므로 UI 문구·코드·문서 모두에서 금지. 다중 파일 일괄 처리 — 범위 밖

**클라이언트 사이드 처리 (Client-side Processing)**:
PDF 회전 연산이 전적으로 사용자의 브라우저에서 수행되는 방식. 서버·스토리지가 존재하지 않는다.

**뷰어 모드 (Viewer Mode)**:
파일이 열린 뒤의 화면 상태. 미리보기가 화면을 지배하고, 상단 바(파일명·페이지 수·다른 파일 선택)와 하단 툴바가 콘텐츠 위에 뜬다. 파일 선택 전에는 환영 화면(중앙 스택)이다.

**툴바 (Toolbar)**:
하단에 떠 있는 반투명 재질(backdrop-filter) 컨트롤 층. `[반시계 | 각도 배지 | 시계 | 파일 이름 | 다운로드]`로 구성되며, 아래에서 스프링으로 떠오르고 같은 경로로 물러난다. 콘텐츠(종이 하단)는 툴바 아래로 지나간다.

**전체 회전 (Whole-document Rotation)**:
문서 내 모든 페이지에 동일한 회전을 일괄 적용하는 것. 이 앱의 유일한 회전 단위이다. 원호 드래그도 모든 페이지를 함께 돌린다.
_Avoid_: 페이지별 회전(per-page rotation) — 범위 밖

**원호 드래그 (Arc Drag)**:
미리보기 페이지를 잡아 비틀어 회전하는 직접 조작 경로. 1:1 추적 → 릴리스 각속도 투사(project) → 최근접 90° 디텐트로 속도 인계 스냅. 스냅 중에도 다시 잡아 이어받을 수 있다. 세로 우세 드래그와 페이지 상·하단 밴드의 가로 드래그는 회전, 중앙 밴드의 가로 드래그는 스트립 스크롤로 판별한다(10px 히스테리시스).

**회전 버튼 (Rotate Buttons)**:
툴바의 시계 90°·반시계 90° 버튼(아이콘+텍스트, 좁은 화면에서는 아이콘만). 원호 드래그와 동일한 상태 경로로 합류하며, 접근성·정밀 조작 경로다. 180°는 같은 버튼을 두 번 눌러 도달한다. 다운로드를 일으키지 않는다.

**누적 회전 (Accumulated Rotation)**:
회전 조작(버튼·원호 드래그)이 쌓여 만들어지는, 문서 전체에 적용될 각도. 정준 표기는 시계 방향 기준 도수(0 | 90 | 180 | 270)이며, 반시계 90°는 270으로 적는다. 0이면 내려받을 변화가 없으므로 다운로드 버튼이 비활성화된다. 화면의 스프링 모션은 이와 별개의 연속 각도(음수·360 초과 허용)를 쓰고, 디텐트 안착 시 정준 표기로 접힌다.
_Avoid_: 자유 각도(임의 각도 저장) — PDF 표준상 90° 배수만 유효. `-90`·`90ccw` 등 비정준 표기

**디텐트 (Detent)**:
스프링이 안착하는 90° 배수 지점. 릴리스 시 각속도를 투사해 가장 가까운 디텐트를 고른다. 디텐트에서만 pdf.js 크리스프 재렌더가 일어난다.

**결과 파일 (Result File)**:
회전이 적용되어 다운로드되는 PDF. 기본 이름은 `{원본명}-rotated-{각도}.pdf` (예: `scan-rotated-270.pdf`)이며, 사용자가 다운로드 전에 툴바의 입력 폼에서 수정할 수 있다. 확장자 `.pdf`는 항상 보장된다.

**상대 회전 (Relative Rotation)**:
회전 각도를 각 페이지의 기존 회전값에 더하는 방식. 사용자가 뷰어에서 보던 모습 기준으로 모든 페이지가 동일하게 돌아간다.
_Avoid_: 절대 회전(absolute rotation) — 회전값 덮어쓰기는 페이지마다 겉보기 결과가 달라지므로 금지

**다운로드 버튼 (Download Button)**:
화면에 단 하나뿐인 채움 주 액션 버튼(툴바 우측). 누르면 누적 회전이 문서 내 모든 페이지에 상대 회전으로 적용된 결과 파일이 다운로드된다.
_Avoid_: 즉시 다운로드(각도 선택 즉시 다운로드) — 이전 모델, 폐기됨

**보호 문서 (Protected Document)**:
열람 암호가 걸린 PDF. 이 앱은 지원하지 않으며, PDF가 아닌 파일·손상된 PDF와 마찬가지로 명확한 오류 메시지 후 다시 파일 선택으로 돌아간다.

**미리보기 (Preview)**:
파일 선택 직후 무대를 채우는 최대 첫 10페이지의 종이 썸네일. 가로 스트립으로 슬라이드하며(스크롤 엣지 페이드), 누적 회전을 실시간으로 반영해 다운로드될 결과와 같은 방향을 미리 보여준다. 회전 모션 중에는 CSS transform으로 연속 회전하고, 디텐트에서 크리스프 재렌더된다.
_Avoid_: 첫 페이지 한 장 미리보기 — 이전 모델, 폐기됨

## 검증

`node verify.mjs` — index.html의 CORE 구간(회전·파일명·검증·스프링·제스처 판별 순수 함수)을 추출해 수치 검증하고, 정적 HTML 계약을 점검한다. 모든 변경은 이 하니스 통과가 전제다. 시각·모션 변경은 스크린샷/프레임 시퀀스를 apple-design.md 기준으로 심사한다.
